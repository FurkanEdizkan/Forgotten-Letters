// Placeholder — the token + primitive showcase lands in Task C5.
export function DesignSystemPage() {
  return (
    <div style={{ padding: 40 }}>
      <h1 className="fl-display">Design System (in progress)</h1>
    </div>
  );
}
