// Placeholder — the full grimdark profile port lands in Task C6.
export function ProfilePage() {
  return (
    <div style={{ padding: 40 }}>
      <h1 className="fl-display">Profile (in progress)</h1>
    </div>
  );
}
